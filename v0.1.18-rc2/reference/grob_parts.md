# Extract specific parts of a `ggplot` or grob

Extract specific parts of a `ggplot` or grob

## Usage

``` r
grob_parts(gplot, parts)
```

## Arguments

- gplot:

  `ggplot` or grob object

- parts:

  names vector of the parts to be extracted.
