# this theme is used across many figures. can be safely removed if update the theme in each function

this theme is used across many figures. can be safely removed if update
the theme in each function

this theme is used across many figures. can be safely removed if update
the theme in each function

## Usage

``` r
theme_osprey(axis_side = "left", fontsize = 4, blank = FALSE)

theme_osprey(axis_side = "left", fontsize = 4, blank = FALSE)
```

## Arguments

- axis_side:

  axis position

- fontsize:

  font size in 'mm'

- blank:

  whether to have blank or background with grids and borders
