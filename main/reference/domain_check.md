# a helper function for g_patient_profile to check whether the domain has data available

a helper function for g_patient_profile to check whether the domain has
data available

## Usage

``` r
domain_check(domain, name)
```

## Arguments

- domain:

  domain input from g_patient_profile

- name:

  domain names
